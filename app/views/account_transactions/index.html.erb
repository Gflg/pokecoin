<p style="color: green"><%= notice %></p>

<div style="text-align:center;">
  <h1>Transactions</h1>

  <% if params[:account_id].nil? %>
    <%= link_to "Back to accounts", accounts_path %>
  <% else %>
    <%= link_to "Back to account", account_path(@account) %>
  <% end %>  
</div>

<div class="container mt-3 mb-4">
  <div class="col-lg-9 mt-4 mt-lg-0">
    <div class="row">
      <div class="col-md-12">
        <div class="user-dashboard-info-box table-responsive mb-0 bg-white p-4 shadow-sm", style="width: 80%; margin-left: 10%;">
          <table class="table manage-candidates-top mb-0">
            <thead>
              <tr>
                <th>Account's username</th>
                <th class="text-center">Pokémon</th>
                <th class="text-center">Dollar price</th>
                <th class="text-center" style="width: 25%">Transaction type</th>
                <th class="text-center" style="width: 30%">Creation date</th>
              </tr>
            </thead>
            <tbody>
              <% @transactions.each do |transaction| %>
                <%= render transaction %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>